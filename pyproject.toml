[project]
name = "discharge-summary-agent"
version = "0.1.0"
description = "Evidence-grounded discharge summary agent"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
